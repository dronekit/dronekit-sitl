machine:
  environment: {}
dependencies:
  override:
    - ./dronekit-sitl --list:
        environment: {}
  pre:
    - python setup.py install:
        environment: {}
test:
  override:
    - 'true'
general:
  build_dir: .
