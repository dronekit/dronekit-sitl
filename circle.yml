machine:
  environment: {}
dependencies:
  override:
    - dronekit-sitl --list:
        environment: {}
  pre:
    - sudo python setup.py install:
        environment: {}
test:
  override:
    - 'true'
general:
  build_dir: .
