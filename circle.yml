machine:
  environment: {}
dependencies:
  pre:
    - sudo python setup.py install
    - sudo pip install nose
  override:
    - cd / && dronekit-sitl --list
    - nosetests tests
test:
  override:
    - 'true'
general:
  build_dir: .
